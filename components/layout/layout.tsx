import React from "react";

const Layout = ({
  children,
  isReview,
}: {
  children: React.ReactNode;
  isReview?: boolean;
}) => {
  return <div className='w-full h-min-screen'></div>;
};

export default Layout;
